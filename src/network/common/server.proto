syntax = "proto3";

// TODO: надо еще почитать по поводу названий пакетов
package common.server;

// не запускал и не тестил, но короче надо +- так прокинуть общий файл
import "contracts.proto"

option go_package = "github.com/matelq/p2pmp/src/network/common/server";

service ServerServer {
  rpc CallFuncOnServer(Text) returns(Text) {}
}

// Файл для сервиса grpc на Клиентской стороне