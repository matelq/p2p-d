syntax = "proto3";

// TODO: надо еще почитать по поводу названий пакетов
package common.client;

// не запускал и не тестил, но короче надо +- так прокинуть общий файл
import "common.proto"

option go_package = "github.com/matelq/p2pmp/src/network/common/client";

service ClientServer {
  rpc CallFuncOnClient(Text) returns(Text) {}
}

// Файл для сервиса grpc на Клиентской стороне